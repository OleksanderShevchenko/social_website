services:
  db:
    image: postgres:16-alpine
    container_name: social_website_db
    restart: always
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5452:5452"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: